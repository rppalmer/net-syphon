# Unsupported browser experiment

Historical source only: not installed, maintained, or part of the default tests.
Original imports and paths are preserved. Do not run it against real account data.

The August 2026 probe failed when Chromium needed an unpermitted temporary directory.
Its denial checks and watchdog were also unreliable: browser isolation was never
established. Any revival requires a new security design and tests, not a passing flag.
